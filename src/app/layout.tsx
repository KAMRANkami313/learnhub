import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'LearnHub — Next-Gen Learning Dashboard',
  description:
    'A beautifully crafted learning dashboard with real-time progress tracking, activity visualization, and personalized learning paths.',
  keywords: ['learning', 'dashboard', 'courses', 'progress', 'supabase'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
